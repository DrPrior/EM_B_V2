.env
.env.local
project_data


# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
ENV/
env/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
.vscode/settings.json
EM_B_Hybrid.code-workspace

# Testing
.pytest_cache/
.coverage
htmlcov/
pytest_output.log
pytest.ini
test_results.txt


# Docker
.dockerignore

# Neo4j graph snapshots (share out-of-band or via Git LFS — a .dump with
# embeddings is large). scripts/export-graph.ps1 writes here.
snapshot/

# Build artifacts
build/
dist/
*.so

# Electron desktop app build outputs (source under electron/ IS committed;
# src/static/vendor/ IS committed so it ships in the image)
electron/node_modules/
electron/dist/
# electron/build/ holds source assets electron-builder consumes (app icons,
# etc.) — re-include it despite the generic build/ rule above.
!electron/build/
!electron/build/**
# Release assets produced by electron/scripts/build-release.ps1
release/
# Assembled USB drive layout produced by scripts/stage-usb.ps1 — regenerable
# build artifacts (installer .exe, api/data tarballs, neo4j.dump). Never commit;
# they blow past GitHub's 100 MB file limit. Share out-of-band or via Git LFS.
usb-staging
# ...and the packaged zip of that layout.
usb-staging.zip


#test outputs and logs
test_output.txt